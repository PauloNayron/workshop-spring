server:
  port: 9000

spring:
  mvc:
    format:
      date=yyyy-MM-dd:
  data:
    mongodb:
      password: password
      username: username
      database: workshop
      port: 27017
      host: localhost
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update